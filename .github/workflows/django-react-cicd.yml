name: CI/CD Pipeline

on:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Debug file paths
        run: |
          echo "Listing all files to find Dockerfile path..."
          ls -R

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 🧠 Build Backend Image (ensure backend/Dockerfile exists)
      - name: Build Backend Image
        run: docker build --no-cache -f backend/Dockerfile -t jahed0307/react-django-backend:latest .


      # 🧠 Build Frontend Image (ensure frontend/Dockerfile exists)
      - name: Build Frontend Image
        run: docker build --no-cache -f frontend/Dockerfile -t jahed0307/react-django-frontend:latest .

      - name: Push Backend Image
        run: docker push jahed0307/react-django-backend:latest

      - name: Push Frontend Image
        run: docker push jahed0307/react-django-frontend:latest

      # 🔹 Optional: Deploy using docker-compose
      - name: Deploy using docker-compose
        run: |
          docker-compose down || true
          docker-compose pull
          docker-compose up -d
